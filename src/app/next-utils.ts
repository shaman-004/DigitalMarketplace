// import next from "next";

// const PORT=Number (process.env.PORT || 3000);

// export const nextApp=next({
//     dev:process.env.NODE_ENV !== 'production',
//     port:PORT
// });

// export const nextHandler=nextApp.getRequestHandler();

// // This function handles all requests to the server. It checks if there is a Next.js page that matches the request, and if so, it renders it.
//  If not, it passes the request to the next middleware in the chain.